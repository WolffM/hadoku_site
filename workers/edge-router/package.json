{
  "name": "edge-router",
  "version": "1.0.0",
  "description": "Edge router Worker for hadoku.me with API fallback logic",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "keywords": ["cloudflare", "worker", "edge", "router"],
  "author": "Hadoku",
  "license": "ISC",
  "dependencies": {
    "@hadoku/worker-utils": "workspace:*",
    "hono": "^4.10.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^4.42.2"
  }
}
