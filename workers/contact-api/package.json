{
  "name": "@hadoku/contact-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply contact-submissions --local",
    "migrate:remote": "wrangler d1 migrations apply contact-submissions --remote",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.10.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "@hadoku/worker-utils": "workspace:*",
    "vitest": "^4.0.4",
    "wrangler": "^4.45.0"
  }
}
